<article class="media">
  <div class="media-content">
    <div class="content">
      <p>
        <a href="{{ item.url }}" target="_blank">{{ item.name }}</a>,<br>
        {{ item.affiliation }}.<br>
        {% if item.url %}
          <a href="{{ item.url }}"></a>.,
        {% endif %}
      </p>
    </div>
  </div>
</article>
